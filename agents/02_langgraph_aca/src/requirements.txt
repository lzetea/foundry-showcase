# LangGraph hosted-agent runtime.
# azure-ai-agentserver-langgraph 1.0.0b17 requires the LangChain 1.x line.
# The versions below are a verified-compatible set (resolved & import-tested
# 2026-06): pydantic 2.13.4 / langchain-core 1.4.8 pulled transitively.
# Do NOT build with pip's global --pre flag -- it pulls broken pre-release
# pydantic/langchain-core and crashes langchain_openai at request time.
azure-ai-agentserver-langgraph==1.0.0b17
azure-identity>=1.19.0
python-dotenv==1.0.1
pandas==2.2.3
langchain==1.3.11
langchain-openai==1.3.3
azure-core-tracing-opentelemetry>=1.0.0b13
